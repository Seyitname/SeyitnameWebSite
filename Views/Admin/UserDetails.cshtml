<!-- AI tarafından yapıldı - Admin Kullanıcı Detayları -->
@model SeyitnameWebSite.Data.User
@{
    ViewData["Title"] = "Kullanıcı Detayları - Admin Paneli";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="text-center mb-5 portfolio-item">
                <h1 class="hero-title mb-2">👤 Kullanıcı Detayları</h1>
                <p class="hero-subtitle">@Model?.UserName</p>
            </div>

            <div class="card-custom portfolio-item">
                <div class="row mb-4">
                    <div class="col-md-6">
                        <h5 class="text-muted">👨‍💻 Kullanıcı Adı</h5>
                        <p class="gradient-text h5">@Model?.UserName</p>
                    </div>
                    <div class="col-md-6">
                        <h5 class="text-muted">🔑 Kullanıcı ID</h5>
                        <p class="text-light small">@Model?.Id</p>
                    </div>
                </div>

                <hr class="border-secondary" />

                <div class="row mb-4">
                    <div class="col-md-6">
                        <h5 class="text-muted">👤 Ad-Soyad</h5>
                        <p class="text-light">@Model?.FullName</p>
                    </div>
                    <div class="col-md-6">
                        <h5 class="text-muted">📧 E-posta</h5>
                        <p class="text-light">@Model?.Email</p>
                    </div>
                </div>

                <hr class="border-secondary" />

                <div class="row mb-4">
                    <div class="col-md-6">
                        <h5 class="text-muted">📅 Üye Olma Tarihi</h5>
                        <p class="text-light">@Model?.CreatedDate.ToString("dd.MM.yyyy HH:mm:ss")</p>
                    </div>
                    <div class="col-md-6">
                        <h5 class="text-muted">📞 Telefon</h5>
                        <p class="text-light">@(string.IsNullOrEmpty(Model?.PhoneNumber) ? "Kayıtlı değil" : Model.PhoneNumber)</p>
                    </div>
                </div>

                <hr class="border-secondary" />

                @if (!string.IsNullOrEmpty(Model?.Bio))
                {
                    <div class="mb-4">
                        <h5 class="text-muted">📝 Biyografi</h5>
                        <p class="text-light">@Model.Bio</p>
                    </div>

                    <hr class="border-secondary" />
                }

                <div class="row mb-4">
                    <div class="col-md-6">
                        <h5 class="text-muted">🔒 İki Faktörlü Kimlik Doğrulama</h5>
                        <p class="text-light">@(Model?.TwoFactorEnabled == true ? "✅ Etkin" : "❌ Kapalı")</p>
                    </div>
                    <div class="col-md-6">
                        <h5 class="text-muted">🔐 E-posta Doğrulandı</h5>
                        <p class="text-light">@(Model?.EmailConfirmed == true ? "✅ Evet" : "❌ Hayır")</p>
                    </div>
                </div>

                <hr class="border-secondary" />

                <div class="d-flex gap-2 flex-wrap">
                    <a href="/Admin/Users" class="btn btn-outline-secondary">
                        ← Üye Listesine Dön
                    </a>
                    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
                        🗑️ Kullanıcıyı Sil
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Delete Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content" style="background-color: rgba(15, 12, 41, 0.95); border: 1px solid rgba(102, 126, 234, 0.3);">
            <div class="modal-header border-secondary">
                <h5 class="modal-title gradient-text" id="deleteModalLabel">⚠️ Kullanıcıyı Kalıcı Olarak Sil</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body text-light">
                <p><strong>@Model?.UserName</strong> kalıcı olarak silinecek!</p>
                <p class="text-warning">⚠️ <strong>Bu işlem geri alınamaz! Tüm veriler kaybolacak!</strong></p>
                <ul class="text-warning small">
                    <li>Profil bilgileri silinecek</li>
                    <li>Hesap erişimi sonlandırılacak</li>
                    <li>Veriler kurtarılamayacak</li>
                </ul>
            </div>
            <div class="modal-footer border-secondary">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">İptal Et</button>
                <button type="button" class="btn btn-danger" id="confirmDeleteBtn">Evet, Kalıcı Olarak Sil</button>
            </div>
        </div>
    </div>
</div>

<script>
document.getElementById('confirmDeleteBtn').addEventListener('click', async function() {
    const userId = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model?.Id));
    
    try {
        const response = await fetch(`/Admin/DeleteUser`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({ id: userId })
        });

        if (response.ok) {
            alert('✅ Kullanıcı başarıyla silindi!');
            window.location.href = '/Admin/Users';
        } else {
            alert('❌ Silme işlemi başarısız!');
        }
    } catch (error) {
        console.error('Hata:', error);
        alert('❌ Bir hata oluştu!');
    }
});
</script>
