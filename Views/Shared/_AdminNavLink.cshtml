<!-- AI tarafından yapıldı - Admin Link Partial View -->
@inject Microsoft.AspNetCore.Identity.UserManager<SeyitnameWebSite.Data.User> UserManager

@{
    var currentUser = await UserManager.GetUserAsync(User);
    var allUsers = UserManager.Users.ToList();
    var isAdmin = allUsers.FirstOrDefault()?.Id == currentUser?.Id;
}

@if (isAdmin)
@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
{
    <li class="nav-item">
        <a class="nav-link nav-link-custom" asp-controller="Admin" asp-action="Users" style="color: #f093fb;">⚙️ Admin</a>
    </li>
}
