@model LoginModel
@{
    ViewData["Title"] = "Giriş Yap";
}

<!-- AI tarafından yapıldı - Modern login sayfası -->

<div class="form-page-bg">
    <div class="form-page-content">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-5">
                    <div class="card-custom shadow-lg">
                        <div class="card-body p-5">
                            <h2 class="card-title text-center gradient-text mb-4">Giriş Yap</h2>

                            <form asp-action="Login" method="post">
                                <div class="form-group mb-3">
                                    <label asp-for="Username" class="form-label gradient-text">👨‍💻 Kullanıcı Adı</label>
                                    <input asp-for="Username" class="form-control-custom form-control" placeholder="Kullanıcı Adı" />
                                    <span asp-validation-for="Username" class="text-danger small"></span>
                                </div>

                                <div class="form-group mb-3">
                                    <label asp-for="Password" class="form-label gradient-text">🔐 Şifre</label>
                                    <input asp-for="Password" type="password" class="form-control-custom form-control" placeholder="Şifre" />
                                    <span asp-validation-for="Password" class="text-danger small"></span>
                                </div>

                                <div class="form-group mb-4">
                                    <div class="form-check">
                                        <input asp-for="RememberMe" type="checkbox" class="form-check-input" />
                                        <label asp-for="RememberMe" class="form-check-label text-muted">Beni Hatırla</label>
                                    </div>
                                </div>

                                @if (!ViewData.ModelState.IsValid && ViewData.ModelState.Values.Any(x => x.Errors.Any()))
                                {
                                    <div class="alert alert-custom alert-danger mb-4">
                                        @Html.ValidationSummary(false)
                                    </div>
                                }

                                <button type="submit" class="btn btn-custom w-100 mb-3 text-white">Giriş Yap</button>
                            </form>

                            <hr class="border-secondary" />

                            <p class="text-center text-muted">
                                Hesabın yok mu? 
                                <a asp-action="Register" class="gradient-text text-decoration-none">Kayıt Ol</a>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
